# Daily Pilot — سند معماری (Architecture)

> **هدف این سند:** مرجع واحد برای درک ساختار، تصمیم‌ها و مسیر آینده‌ی پروژه.
> این سند «زنده» است — هر تغییر معماری مهم باید همین‌جا ثبت شود.

| وضعیت بخش | معنی |
|---|---|
| ✅ کامل | نوشته و تأیید شده |
| 🟡 پیش‌نویس | نوشته شده ولی هنوز قطعی نشده |
| ⬜ خالی | هنوز نوشته نشده |

**تاریخ آخرین به‌روزرسانی:** ۱۴۰۴/۰۶/۱۶ — نسخه ۰.۱ (اسکلت)

---

## فهرست مطالب

1. [نمای کلی محصول (Overview)](#۱-نمای-کلی-محصول)
2. [پشته فناوری (Tech Stack)](#۲-پشته-فناوری)
3. [معماری کلان سیستم (System Architecture)](#۳-معماری-کلان-سیستم)
4. [ساختار پوشه‌ها (Directory Structure)](#۴-ساختار-پوشه‌ها)
5. [مدل داده (Data Model)](#۵-مدل-داده)
6. [طراحی API (API Design)](#۶-طراحی-api)
7. [احراز هویت و امنیت (Auth & Security)](#۷-احراز-هویت-و-امنیت)
8. [منطق دامنه اصلی (Core Domain Logic)](#۸-منطق-دامنه-اصلی)
9. [معماری فرانت‌اند (Frontend Architecture)](#۹-معماری-فرانت‌اند)
10. [PWA و حالت آفلاین (PWA & Offline)](#۱۰-pwa-و-حالت-آفلاین)
11. [یکپارچه‌سازی‌های بیرونی (Integrations)](#۱۱-یکپارچهسازیهای-بیرونی)
12. [قراردادها و کنوانسیون‌ها (Conventions)](#۱۲-قراردادها-و-کنوانسیونها)
13. [محیط‌ها و استقرار (Environments & Deployment)](#۱۳-محیطها-و-استقرار)
14. [تست و کیفیت (Testing & Quality)](#۱۴-تست-و-کیفیت)
15. [دفتر تصمیم‌ها و نقشه راه (Decisions & Roadmap)](#۱۵-دفتر-تصمیمها-و-نقشه-راه)

---

## ۱. نمای کلی محصول ⬜

**چه چیزی مستقر می‌شود:**
- یک پاراگراف: Daily Pilot چیست و چه مسئله‌ای را حل می‌کند
- کاربر هدف و سناریوی اصلی استفاده
- قابلیت‌های کلیدی (ثبت تسک، برنامه‌ریزی روزانه، بودجه‌بندی زمان، تحلیل AI، نصب موبایل)
- آنچه Daily Pilot **نیست** (تعیین مرز محصول)

**وضعیت:** ⬜ خالی

---

## ۲. پشته فناوری ⬜

**چه چیزی مستقر می‌شود:**
- فهرست فناوری‌ها همراه با «چرا»ی انتخاب هرکدام
- نسخه‌های قفل‌شده و نکات ارتقا

**پیش‌نویس بر اساس `package.json`:**

| لایه | فناوری | نسخه |
|---|---|---|
| فریم‌ورک | Next.js (App Router) | 13.5.6 |
| زبان | TypeScript | 5.x |
| UI | React | 18.x |
| استایل | CSS Modules + CSS Variables (globals.css) | — |
| انیمیشن | framer-motion | 11.x |
| فرم | react-hook-form + zod + @hookform/resolvers | — |
| دیتابیس | Prisma ORM | 5.x |
| هش/توکن | bcrypt + jsonwebtoken | — |
| تقویم | moment-jalaali | 0.10.x |
| آیکون | lucide-react | — |
| QR | qrcode | — |
| نوتیفیکیشن | react-toastify | — |

**سؤال‌های باز:**
- [ ] آیا Tailwind به‌عنوان لایه‌ی کمکی وارد شود یا CSS خالص بماند؟ (الان `tailwind.config.ts` خالی است)
- [ ] ارائه‌دهنده‌ی ایمیل برای OTP (Resend؟) و ارائه‌دهنده‌ی AI قطعی شوند

**وضعیت:** ⬜ خالی (جدول بالا پیش‌نویس خام است)

---

## ۳. معماری کلان سیستم ⬜

**چه چیزی مستقر می‌شود:**
- دیاگرام جریان: مرورگر/PWA → Next.js API Routes → Prisma → دیتابیس
- سرویس‌های بیرونی (ایمیل، AI) و محل فراخوانی آن‌ها
- تقسیم مسئولیت: سرور در برابر کلاینت، نقش localStorage در آفلاین

**نمودار پیش‌نویس (متنی):**

```
[PWA / Browser]
   │  (fetch + offline queue در localStorage)
   ▼
[Next.js App Router — API Routes]
   ├── /api/auth/*      ← JWT + bcrypt (+ OTP آینده)
   ├── /api/tasks/*     ← CRUD + analyze + rollover
   ├── /api/planner/*   ← برنامه روزانه
   ├── /api/ai/*        ← تحلیل (نیازمند اینترنت)
   ▼
[Prisma Client] → [Database]
   ▲
[سرویس‌های بیرونی: ایمیل OTP، AI provider]
```

**سؤال‌های باز:**
- [ ] دیتابیس محصول: کدام؟ (Postgres/SQLite/…)
- [ ] آیا AI سمت سرور فراخوانی می‌شود یا کلاینت؟ (الان `/api/ai/test` موجود است)

**وضعیت:** ⬜ خالی

---

## ۴. ساختار پوشه‌ها ⬜

**چه چیزی مستقر می‌شود:**
- نقشه‌ی `app/` و مسئولیت هر پوشه
- قاعده‌ی «فایل جدید کجا برود؟»

**پیش‌نویس بر اساس ساختار فعلی:**

```
app/
├── api/          ← Route Handlers (بک‌اند)
│   ├── auth/     ← login, register, logout, profile, avatar, change-password
│   ├── tasks/    ← CRUD، [id]/analyze، [id]/complete، overdue، rollover
│   ├── planner/  ← day، history
│   └── ai/       ← تحلیل AI (test فعلاً)
├── auth/         ← صفحات ورود/ثبت‌نام
├── dashboard/    ← داشبورد و تنظیمات (محافظت‌شده)
├── components/   ← کامپوننت‌های مشترک (Header، Avatar، motion، landing، task، calender، pwa)
├── contexts/     ← Contextهای کلاینت (SettingsContext)
├── hooks/        ← UseDaySummary، useHistoryMarkers، useInstallPrompt
├── lib/          ← getCurrentUser، getPrisma، jalili، offline، rateLimit، time، ai، planner
├── schema/       ← اسکیماهای zod فرم‌ها
└── types/        ← تایپ‌های مشترک
prisma/           ← schema.prisma + مهاجرت‌ها
public/           ← manifest، sw.js، آیکون‌ها
scripts/          ← generate-icons.mjs
```

**سؤال‌های باز:**
- [ ] قاعده نام‌گذاری فایل‌های کامپوننت (PascalCase) و hooks (camelCase با پیشوند use) قطعی شود

**وضعیت:** ⬜ خالی

---

## ۵. مدل داده ⬜

**چه چیزی مستقر می‌شود:**
- دیاگرام روابط مدل‌ها + جدول فیلدهای مهم
- قواعد ایندکس، حذف آبشاری، و کلیدهای یکتا

**پیش‌نویس (مدل‌های موجود در `prisma/schema.prisma`):**
- `User` — id، username، ایمیل، هش رمز، آواتار، تاریخ تولد، …
- `Task` — id، text، زمان‌ها، وضعیت، ارتباط با کاربر و برنامه روز
- `DailyPlan` — dayKey (کلید روز جلالی)، …
- (مدل‌های OTP آینده: `OtpCode` — کد هش‌شده، expire، مصرف‌شده)

**سؤال‌های باز:**
- [ ] فرمت ذخیره‌ی تاریخ تولد در DB (استاندارد ISO؟) با نمایش شمسی در UI
- [ ] مدل OtpCode برای ورود دو مرحله‌ای

**وضعیت:** ⬜ خالی

---

## ۶. طراحی API ⬜

**چه چیزی مستقر می‌شود:**
- جدول همه‌ی اندپوینت‌ها: متد، مسیر، احراز هویت، ورودی/خروجی، خطاها
- قالب پاسخ موفق/خطا و کدهای وضعیت

**پیش‌نویس بر اساس Route Handlers موجود:**

| مسیر | متد | احراز هویت | توضیح |
|---|---|---|---|
| `/api/auth/register` | POST | عمومی | ثبت‌نام |
| `/api/auth/login` | POST | عمومی | ورود (← دو مرحله‌ای با OTP) |
| `/api/auth/logout` | POST | لازم | خروج |
| `/api/auth/profile` | GET/PATCH | لازم | پروفایل |
| `/api/auth/avatar` | POST | لازم | آپلود آواتار |
| `/api/auth/change-password` | POST | لازم | تغییر رمز |
| `/api/tasks` | GET/POST | لازم | لیست/ایجاد تسک |
| `/api/tasks/[id]` | PATCH/DELETE | لازم | ویرایش/حذف |
| `/api/tasks/[id]/complete` | POST | لازم | تکمیل تسک |
| `/api/tasks/[id]/analyze` | POST | لازم | تحلیل AI تسک |
| `/api/tasks/overdue` | GET | لازم | عقب‌افتاده‌ها |
| `/api/tasks/rollover` | POST | لازم | انتقال به فردا |
| `/api/planner/day` | GET | لازم | برنامه‌ی روز |
| `/api/planner/history` | GET | لازم | تاریخچه |
| `/api/ai/test` | POST | ⚠️ بدون محافظ | فقط توسعه — قبل انتشار حذف/محافظ شود |

**وضعیت:** ⬜ خالی

---

## ۷. احراز هویت و امنیت ⬜

**چه چیزی مستقر می‌شود:**
- چرخه‌ی کامل JWT: صادر شدن، ذخیره (کوکی؟)، اعتبارسنجی (`getCurrentUser`)
- bcrypt و حداقل استانداردهای رمز
- rate limit (`lib/rateLimit.ts`) — کجا و چطور
- جریان OTP دو مرحله‌ای (طرح: کد ۶ رقمی، انقضا، یک‌بارمصرف، محدودیت ارسال مجدد، ذخیره‌ی هش‌شده)
- نقش middleware و محافظت از روت‌های داشبورد

**سؤال‌های باز:**
- [ ] TTL توکن و رفرش توکن؟
- [ ] OTP فعلاً UI دارد ولی بک‌اند ناقص است — این بخش باید در همین سند قطعی شود

**وضعیت:** ⬜ خالی

---

## ۸. منطق دامنه اصلی ⬜

**چه چیزی مستقر می‌شود:**
- قوانین برنامه‌ریز روزانه (planner) و «بودجه‌ی زمانی»
- dayKey جلالی: تولید، مقایسه، مرز روز
- سناریوی rollover و overdue
- محاسبات DaySummary (بودجه امروز، مصرف‌شده، ذخیره‌شده، وضعیت برنامه)
- رفتار AI وقتی آفلاین است (نمایش «—» و پیام مناسب)

**سؤال‌های باز:**
- [ ] مرز روز: نیمه‌شب محلی یا تهران؟ تغییر ساعت تابستانی؟

**وضعیت:** ⬜ خالی

---

## ۹. معماری فرانت‌اند ⬜

**چه چیزی مستقر می‌شود:**
- الگوی داده: Server Components در برابر Client Components — کجا هرکدام
- Contexts موجود (SettingsContext) و قاعده‌ی عدم تکرار state سرور
- سیستم انیمیشن: `AnimatedModal`، `Reveal`، الگوهای framer-motion و مقررات استفاده
- تم روشن/تیره با CSS Variables و data-theme
- RTL و فونت فارسی

**سؤال‌های باز:**
- [ ] آیا state manager دیگری لازم است یا Context + hook کافی است؟ (پیشنهاد: کافی)

**وضعیت:** ⬜ خالی

---

## ۱۰. PWA و حالت آفلاین ⬜

**چه چیزی مستقر می‌شود:**
- manifest (نام، آیکون‌ها، start_url) و الزامات نصب در اندروید/iOS
- Service Worker (`public/sw.js`): استراتژی کش برای شل اپ و APIها
- جریان آفلاین: کش تسک‌ها در localStorage، صف عملیات، همگام‌سازی
- Install Prompt (`useInstallPrompt`، `InstallCard`، بخش دانود لندینگ)
- OfflineIndicator

**سؤال‌های باز:**
- [ ] `manifest.start_url` الان `/dashboard` است — برای کاربر واردنشده عجیب است؛ قطعی شود
- [ ] استراتژی کش دقیق: cache-first برای شل، network-first برای API؟

**وضعیت:** ⬜ خالی

---

## ۱۱. یکپارچه‌سازی‌های بیرونی ⬜

**چه چیزی مستقر می‌شود:**
- ارائه‌دهنده‌ی AI: کدام سرویس، از کدام لایه فراخوانی می‌شود، کلید لازم
- ایمیل (OTP): Resend پیشنهاد شده — کلید `RESEND_API_KEY`، قالب ایمیل کد
- جدول متغیرهای محیطی موردنیاز (نام کلیدها بدون مقدار)

**سؤال‌های باز:**
- [ ] انتخاب نهایی سرویس AI و ایمیل + ثبت در این بخش

**وضعیت:** ⬜ خالی

---

## ۱۲. قراردادها و کنوانسیون‌ها ⬜

**چه چیزی مستقر می‌شود:**
- نام‌گذاری فایل‌ها و کامپوننت‌ها
- قالب پیام‌های خطا و Toastها (فارسی، لحن یکسان)
- قواعد استایل: توکن‌های CSS (`--space-*`، `--shadow-*`، …) و ممنوعیت مقادیر خام
- قواعد انیمیشن: durationهای مجاز، جلوگیری از انیمیشن‌های سنگین

**وضعیت:** ⬜ خالی

---

## ۱۳. محیط‌ها و استقرار ⬜

**چه چیزی مستقر می‌شود:**
- دستورهای dev/build/start (`npm run …`)
- متغیرهای محیطی موردنیاز (فقط نام کلیدها): DATABASE_URL، JWT_SECRET، RESEND_API_KEY، کلید AI، …
- مراحل استقرار و ملاحظات build (تولید Prisma Client، اجرای مهاجرت‌ها)
- تفاوت تنظیمات dev و production

**وضعیت:** ⬜ خالی

---

## ۱۴. تست و کیفیت ⬜

**چه چیزی مستقر می‌شود:**
- وضعیت فعلی: بدون تست خودکار؛ گیت‌های فعلی = tsc + eslint
- طرح تست پیشنهادی: از کجا شروع شود (توصیه: مسیرهای auth و منطق planner)
- ابزار پیشنهادی و حداقل پوشش هدف

**سؤال‌های باز:**
- [ ] آیا Vitest اضافه شود؟ (اولین تست روی `lib/jalili.ts` و `lib/planner` معنادار است)

**وضعیت:** ⬜ خالی

---

## ۱۵. دفتر تصمیم‌ها و نقشه راه ⬜

**چه چیزی مستقر می‌شود:**
- **دفتر تصمیم‌ها (ADR سبک):** هر تصمیم مهم با «چرا» — مثلاً: App Router به‌جای Pages، JWT به‌جای سشن، moment-jalaali، فریم‌ورک انیمیشن موجود (framer-motion) بدون اضافه‌کردن وابسته‌ی تکراری
- **نقشه راه:** آیتم‌های باقی‌مانده با اولویت

**پیش‌نویس نقشه راه (از بازبینی اخیر):**
1. تکمیل بک‌اند OTP (ارسال، اعتبارسنجی، انقضا، rate limit)
2. اصلاح `manifest.start_url`
3. انتخابگر تاریخ تولد شمسی در تنظیمات
4. تصمیم درباره‌ی `/api/ai/test` (حذف یا محافظت)
5. افزودن حداقل تست برای auth و planner

**وضعیت:** ⬜ خالی

---

## فرآیند نگهداری این سند

- هر بخش پس از نوشتن و تأیید، وضعیتش ✅ می‌شود
- تغییر معماری مهم ⇒ ابتدا این سند، بعد کد
- بازبینی دوره‌ای: پس از هر ویژگی بزرگ، بخش‌های مرتبط به‌روز شوند
